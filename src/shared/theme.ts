import { createTheme } from '@mui/material';

export const theme = createTheme({
  typography: {
    allVariants: {
      fontFamily: 'Manrope',
    },
    button: {
      width: '100%',
      maxWidth: 400,
      backgroundColor: 'lavender',
      color: 'grey',
    },
  },
});
